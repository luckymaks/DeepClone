{
  "name": "deepclone",
  "version": "1.0.0",
  "description": "A deep clone implementation that avoids exponential blowup on object graphs with shared references, using a WeakMap to clone each object exactly once.",
  "type": "module",
  "main": "main.js",
  "license": "MIT",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/.bin/jest"
  },
  "devDependencies": {
    "@jest/globals": "^30.4.1",
    "jest": "^30.4.2"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
